# Fake News Detection
Classifies news articles as Fake or Real using Python, TF-IDF, and Logistic Regression.

## Dataset
Uses True.csv and Fake.csv (include these in the repo).

## How to Run
1. Install dependencies: `pip install pandas scikit-learn`
2. Run the notebook/script in Python
